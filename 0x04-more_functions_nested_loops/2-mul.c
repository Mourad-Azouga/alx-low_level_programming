#include "main.h"
/**
 * mul - checks if the character is upperbase
 * @a: - the first int
 * @b: - the second int
 * @m - the multiple
 * Return: the multiple
 */
int mul(int a, int b)
{
int m;
	m = (a * b);
return (m);
}
